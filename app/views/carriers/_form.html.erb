<%= form_for(@carrier, :html => { :class => "form-horizontal"} ) do |f| %>
    <%= render :partial => 'formdetails', :locals=>{:f => f}%>
    <div class="form-actions">
      <%= f.submit 'Add Carrier', :class=>'btn btn-success' %>   <%= link_to "Cancel", carriers_path, :class=>"btn btn-inverse" %>
    </div>
<% end %>
