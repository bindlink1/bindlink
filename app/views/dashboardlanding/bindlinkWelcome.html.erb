
<%= (if agent_signed_in?  then render 'agentdashboard' end) %> <%= (if underwriter_signed_in?  then render 'underwriterdashboard' end) %> <%= (if admin_signed_in?  then render 'admindashboard' end) %>
