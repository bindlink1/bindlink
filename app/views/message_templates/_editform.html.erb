<%= form_for @template, :remote =>true do |f| %>
     <table style="width: 100%;">

      <col width="10%" />
      <col width="10%" />
      <col width="20%" />
      <col width="60%" />
      <tbody>
      <%= render('editformfields', :f => f) %>
      </tbody>
    </table>

<% end %>
