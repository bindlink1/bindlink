<div class="alert alert-error">
  <button type="button" class="close" data-dismiss="alert">×</button>
  <strong>Warning!</strong> This action will change the Producing Agency this policy is associated with.
</div>
<%= form_for @policy,:method=>"POST" ,:url=>updateagentofrecord_path(:id=>@policy.id), :html=>{:class=>'form-horizontal'}, :remote => true do |f| %>
    <div class="control-group">
      <label class="control-label">Producing Agency</label>
      <div class="controls">
        <%= collection_select :policy, :producingagency_id, @producingagencies,:id, :nameandcode, {:include_blank => 'Select One'}%>
      </div>
    </div>
    <div class="controls">
      <%= f.submit 'Save', :class=>"btn btn-success btn-small", :id=>"aorfrmbtn"%> <button class="btn btn-small" id="aorcancel" onclick='return false;'>Cancel</button>
    </div>
<% end %>
<hr>