<%= form_for(@agent) do |f| %>
  <% if @agent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agent.errors.count, "error") %> prohibited this Agent Data from being saved:</h2>

      <ul>
      <% @agent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br />
    <%= f.text_field :last_name %>
  </div>
  
  <div class="field">
  	<%= f.label :agency_id%><br />
  	<%= collection_select( :agent, :agency_id, Agency.all, :id, :agency_name) %>

  </div>
    <div class="field">
    <%= f.label :email%><br />
    <%= f.text_field :email %>
  </div>

  
</br>
  <div class="actions">
    <%= f.submit %>
  </div>
</br>
<% end %>
