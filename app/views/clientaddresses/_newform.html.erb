<div class="wellwhitemenu">
	<h3>Create a new Address</h3> <button type="button" class="close" data_dismiss="modal" aria_hidden="true" id ="closeit">×</button>
</div>

<%= form_for [@client,@clientaddress], :html=>{:class =>"form-horizontal"} , :remote =>true do |f| %>
	<%= render('formfields', :f => f) %>
<% end %>

<script>
	$("#clientaddress_zip").keyup(function() {
        var el = $(this);

        if (el.val().length === 5) {
            $.ajax({
                url: "https://ziptasticapi.com/"+el.val(),
                cache: false,
                dataType: "json",
                type: "GET",
                success: function(result, success) {
                    $("#clientaddress_city").val(result.city);
                    $("#clientaddress_state").val(result.state);
                }
            });
        }
    });
</script>